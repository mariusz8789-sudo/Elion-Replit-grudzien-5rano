node_modules/
dist/
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json
*.log
*.tsbuildinfo

# Trwały magazyn backendu (SQLite) — dane runtime, nie do repo
packages/backend/data/

# heavy-engine scientific artifacts (runtime, not source)
_dock*/
*.pdbqt

# Delivery archive and local Chromium capture helpers are generated outside the source review path.
artifacts/*.zip
artifacts/*.mjs
# GOV-DRUG-DISCOVERY-E2E-01 demo package (video + rendered report): regenerated on demand by
# scripts/gov-drug-discovery-e2e-demo-capture.mjs, not source.
artifacts/gov-drug-discovery-e2e-demo/
# Investor demo capture (video), regenerated by scripts/genesis-investor-demo-capture.mjs
artifacts/genesis-investor-demo/
# Guide film capture (video), regenerated by scripts/genesis-guide-film-capture.mjs
artifacts/genesis-guide-film/
# HERO capture points (stills + manifest), regenerated by scripts/genesis-hero-capture-points.mjs
# against a live run — the manifest describes that run, so it is never a source artefact.
artifacts/hero-capture/

# Python bytecode from compute workers (packages/backend/src/compute/*.py)
__pycache__/
*.pyc

# Voice guide recordings are generated (npm run voice:lines + scripts/genesis_tts.py), never committed.
packages/frontend/public/audio/*.mp3
packages/frontend/public/audio/manifest.json
packages/frontend/public/audio/lines.json

# Playwright: Mythos Matrix HUD screenshots (scripts/capture-matrix-screenshots.mjs, matrix.e2e.spec.ts) and runner output
artifacts/matrix-*.png
test-results/
playwright-report/

artifacts/clockwork-*.png
artifacts/collider-*.png
artifacts/lab-fpv-*.png
artifacts/cern-complex-*.png
# CERN complex e2e (packages/e2e/src/cern-complex.e2e.spec.ts): the four fidelity-gated screenshots
artifacts/walk-5d.png
artifacts/glass-refraction.png
artifacts/console-pbr.png
artifacts/tunnel-volumetric.png
artifacts/scientific-worlds-*.png
artifacts/human-biology-lab-*.png
# Molecule World Smart UI e2e (packages/e2e/src/molecule-world.e2e.spec.ts)
artifacts/molecule-world.png
artifacts/molecule-world-mobile.png

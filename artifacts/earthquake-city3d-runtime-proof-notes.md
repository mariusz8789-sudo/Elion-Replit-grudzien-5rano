# Earthquake → City3D Runtime Proof Notes

**Captured:** 2026-08-26, Chromium at 1920×1080 on `#/city3d`.

The actual City3D command center rendered exactly **one** `.city-3d-canvas`. The synthetic Earthquake panel was present in the existing right rail, not in a second view. After activating the declared synthetic preset, the panel visibly reported `OVERLAY ACTIVE`, `REPLAY MATCH`, `EVIDENCE COMPLETE`, five mapped fixture sites, a visible SHA-256 evidence digest and mapping fingerprint, plus the `NOT_MODELED (5)` disclosure. The captured City view visibly contained the subtle cyan/red scenario markers in the same renderer.

The scripted clear action then reported `OVERLAY CLEARED` while the one City3D canvas remained present and no WebGL failure state was shown. The structured machine-readable companion is `earthquake-city3d-runtime-proof.json`; the active-state screenshot is `screenshots/city3d-earthquake-demonstrator-1920x1080.png`.

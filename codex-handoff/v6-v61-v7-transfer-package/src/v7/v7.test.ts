import { describe,it,expect } from 'vitest'; import { deterministic } from '../deterministic'; import { createReferenceHumanManifest } from './humanManifest'; import { validateHierarchyScaleMonotonicity } from './humanLod'; import { HyperscopeNavigator } from './hyperscopeNavigator';
describe('V7 human macro micro',()=>{it('has monotonic hierarchy',()=>{const m=createReferenceHumanManifest(deterministic); expect(validateHierarchyScaleMonotonicity(m.nodes)).toBe(true); const n=new HyperscopeNavigator(m); expect(n.descend('heart','myocardium').allowed).toBe(true); expect(n.pathTo('atp-synthase').length).toBe(7);});});

# Genesis Master Execution Status

## Current product decision

Genesis is being advanced as one **Scientific Discovery OS**, not as a collection of disconnected demos. The execution rule is one capability at a time:

`Science Chat → StructuredExperimentRequest → confirmation → existing model/solver → result → visualization/lab/world → provenance → honest Evidence/Replay status`

Every milestone must pass tests, TypeScript, lint, production build, Chromium smoke, GitHub Actions and clean-state publication before the next milestone begins.

## Connected milestones on LIVE

| Capability | Status | Real route/model | Honest boundary |
|---|---|---|---|
| Epidemic city | DONE for current integration contract | `epidemic-city` → `#/city3d` via same-world handoff | Deterministic synthetic city; not a real-world forecast. Evidence protocol and counterfactual require explicit setup. |
| Earthquake | DONE reference vertical slice | `earthquake-scenario` → existing City3D live-world path | Scenario mapping; structural damage is `NOT_MODELED`; no real GIS or building inventory. |
| Minkowski 1+1D | DONE bounded lab flow | `spacetime-minkowski` → Space-Time Lab | Diagram and observables in bounded 1+1D; not a time machine. |
| Schwarzschild geodesic | DONE bounded lab flow | `einstein-schwarzschild-geodesic` → Einstein Lab | Photon geodesic model; no Kerr, no full ray tracer, no time travel. |
| c-Slider | DONE bounded lab flow | `spacetime-c-slider` → Space-Time Lab | Thought experiment with hypothetical c; does not alter physical constants. |
| Particle energy | DONE bounded lab flow | `particle-relativistic-energy` → Particle Lab | Free-particle E=γmc² graph; not a collider or detector experiment. |
| Stellar scaling | DONE bounded lab flow | `universe-starlife` → Universe Lab | Scaling relation with explicit simplifications; not full stellar evolution or prediction. |
| Galaxy rotation curve | DONE bounded lab flow | `universe-rotation-curve` → Universe Lab (`rotationcurve`) | Analytic exponential disk plus pseudo-isothermal halo or MOND alternative; not a fit to any named galaxy and not a CDM-vs-MOND verdict. |
| Galaxy collision | DONE bounded lab flow | `universe-galaxy-collision` → Universe Lab (`collision`) | Restricted Toomre–Toomre two-core plus test particles; not full N-body, hydrodynamics or reconstruction of a real merger. |
| Schwarzschild radius | DONE bounded analytical flow | `einstein-schwarzschild` → Einstein Lab | Non-rotating, uncharged Schwarzschild metric calculation; not Kerr, not a ray tracer and not observational inference. |

## Partial or not connected yet

| Area | Status | Decision |
|---|---|---|
| Formal Evidence Packs | PARTIAL | Current capsules honestly return `PROTOCOL_REQUIRED` unless a protocol exists. Science Chat now opens the existing Evidence & Replay panel directly; do not fabricate packs. |
| A/B and counterfactual UX | PARTIAL | Existing comparisons are available only where two real, compatible runs exist; Science Chat now exposes the Evidence/Replay surface, while missing variants remain `VARIANT_REQUIRED`. |
| Science Chat visual proof / Epidemic handoff | CONNECTED with proof boundary | Confirmed run now hands the same `EpidemicCitySimulation` reference to `#/city3d`; visual proof remains environment-dependent when WebGL is unavailable. |
| Series observations | CONNECTED with honest boundary | Existing protocol evidence `allRuns` is analyzed by the existing `analyseExperimentSeries` contract and surfaced in Experiment Pilot; strong correlation is only `REQUIRES_SCIENTIFIC_REVIEW`, while insufficient data and no-threshold observations remain explicit. |
| Backend provenance transparency | CONNECTED | Backend-routed Pilot runs now expose canonical backendRunId, engine, model version, honesty and classification from the existing provenance contract; the UI does not reconstruct or upgrade claims. |
| Science Chat → Protocol / A-B | CONNECTED | Deterministic Science Chat phrases open the existing Pilot in `mode=protocol`; navigation is read-only and does not create a protocol or start a run. |
| Scientific Memory history surface | CONNECTED with local boundary | Existing `scienceMemory.ts` records are now visible through a product route and Home navigation; users can reopen a saved run with its parameters, export its local JSON record or delete it. It is not cloud history, shared collaboration or an Evidence Pack. |
| Generic executor honesty | CONNECTED | Earthquake’s generic Fabric path now returns an explicit `capability_seam` instead of a false engine failure, while rejected/failed REAL_ENGINE runs receive `engine-not-available` provenance rather than `real-engine`. The existing Earthquake command-center adapter remains the only executable path. |
| Science Chat → Scientific Memory | CONNECTED with local boundary | Deterministic chat commands open the existing `#/memory` history surface; they do not list fabricated records, create cloud state or claim replay evidence. |
| Investor demo proof | DOCUMENTED | `docs/GENESIS_INVESTOR_DEMO_RUNBOOK.md` describes only verified routes, prompts, capabilities and limitations; it is a demo guide, not a claim of additional connectivity. |
| Scientific Control Plane research | STRATEGIC RESEARCH TRACK | `docs/RESEARCH_SELF_DRIVING_LABORATORY_INPUT.md` is research input only. Neutral instrument orchestration, typed observations, QC, chain of custody and wet-lab execution remain `DESIGNED`/`VERIFY_REQUIRED`, not connected capabilities. |
| First Control Plane PySCF benchmark | CONNECTED_LOCALLY / CI_VERIFY_REQUIRED | Canonical async backend execution now runs real H₂ RHF `sto-3g` and `6-31g` through PySCF 2.14.0, creates the existing Evidence Pack, and proves deterministic local rerun `MATCH` with stable run fingerprints and pack identity. The persisted user-facing replay action and CI runtime portability remain open gates; fixed-geometry Hartree–Fock only. See `docs/GENESIS_FIRST_CONTROL_PLANE_BENCHMARK_ADMISSION.md`. |
| Discovery Timeline | NOT A SOLVER | Keep as narrative visualization until a separately bounded model contract exists. |
| Wormholes, grandfather paradox, physical time travel | PARKED | Unsupported or hypothetical; keep outside real solver path. |
| Kerr full 3D / 4D or 5D claims | PARKED | Add only with a separately validated model and explicit limitations. |
| Collider / Matrix / second renderer or world | PARKED | Forbidden until architecture and value case are separately approved. |
| GIS, OSM/DEM, live external data | PARKED | Requires provenance, licensing, ingestion and security design; no live fetch in current Genesis contract. |

## Current LIVE HEAD

The latest published code commit is `87bb562` on `manus/high-fidelity-epidemic-digital-twin`. The latest CI for that commit is `33091111045` and is green. The accepted Claude handoff is `a23e904` (Epidemic→City3D same-world handoff plus routing proofs), the Evidence/Replay entry point is `460c5cf`, the ontology audit and package contract are `c6f612b`, the visual language is `430a0b1`, the preregistered Protocol / A-B designer is `d4c6acd`, the WHY / Next Experiment layer is `5154b71`, the Campaign read-only adapter is `838ef42`, the Campaign reference fixture is `ce9efc6`, the water-pump-pipe Science Chat expose is `6952df6`, the PySCF H₂ RHF chemistry expose is `946f076`, the quantum-tunneling Science Chat expose is `b390e16`, the Earthquake Pilot honesty fix is `be529d3`, the CI/GIS boundary fixes are `7ad97ef`, and the Experiment Pilot Series Observation adapter is `a172487` (CI `33084839359`), backend provenance transparency is `461ee77` (CI `33085822920`), and Science Chat → Protocol/A-B navigation is `9534d8d` (CI `33086705393`); the synchronized roadmap commit is `5bc82bf` (CI `33086913034`), and Scientific Memory history surface is `b981a8c` (CI `33088364819`), and Claude’s generic executor/provenance honesty fix is `0efeb31` (CI `33089056314`), and Science Chat → Scientific Memory is `323b272` (CI `33090391525`), and the investor demo runbook is `52c7b87` (CI `33090916917`); the final roadmap sync is `87bb562` (CI `33091111045`).

## Next CTO gate

The visual-proof pass, Evidence/Replay entry point, ontology audit, package orchestration contract, Genesis Observatory visual refresh, preregistered Protocol / A-B designer, WHY / Next Experiment layer, Campaign read-only entry point, Campaign reference fixture, water-pump-pipe Science Chat expose, PySCF H₂ RHF chemistry expose, quantum-tunneling Science Chat expose, Earthquake Pilot honesty fix, CI/GIS boundary fixes, Series Observation adapter, backend provenance transparency, Science Chat → Protocol/A-B navigation, Scientific Memory history surface, generic executor honesty, Science Chat → Scientific Memory and the investor demo proof are complete. The current roadmap is synchronized to LIVE `87bb562`; no additional scientific capability is admitted by this documentation change. The Protocol UI exposes explicit hypothesis, baseline, sweep parameter, primary metric output key, repetitions and falsification relation, then calls only existing `designScientificExperiment` / `executeScientificExperiment`. WHY uses only recorded arm/run/provenance data, labels the assessment and proposes one bounded next step with `AUTO-RUN: DISABLED`; it does not invent results or variants. Science Chat now opens the existing CampaignScreen with typed `OPEN_CAMPAIGN`, but never creates or starts a campaign. Campaign Evidence → Fabric Evidence Pack / RO-Crate interoperability is formally `BLOCKED/PARKED`: the persisted Campaign contract is real, but no lossless, versioned mapping into the stricter Fabric chain has been proven. The contract-only Campaign fixture confirms this loss and remains `MANUAL_REVIEW`; it has no scientific output and cannot be exported. The water-pump-pipe model is connected through the existing Darcy–Weisbach/Swamee–Jain engineering graph, with explicit `not CFD` limitation. PySCF H₂ RHF is connected through the existing backend real-engine evidence path and remains bounded to fixed-geometry Hartree–Fock; it is not a full molecular dynamics, materials, docking, ADMET or therapeutic claim. Quantum tunneling is connected through the existing shared 1D split-step Fourier runner and Quantum Lab route; it is not a general Schrödinger solver, 2D/3D material model or device prediction. The Phase 3/4 Spacetime Navigator is a parked specification only; no time slider, split-screen world or future prediction is connected. Earthquake in Pilot now uses the existing command-center confirmation path, so generic Pilot does not promise a real run that the generic executor cannot deliver. Experiment Pilot now surfaces the existing `analyseExperimentSeries` observation contract over protocol `allRuns`; it does not rerun, infer causality, or call an observation an `odkrycie`. Backend-routed runs also expose the canonical backend provenance fields in the result view, preserving the distinction between a real engine result and a locally reconstructed claim. Science Chat can now open the existing Protocol/A-B designer directly; the route is read-only until explicit user confirmation. Scientific Memory has a visible Home entry and history route backed by the existing local storage contract; it reopens parameters through the existing scenario bridge and makes no cloud-sharing or reproducibility claim. The generic Fabric executor now reports the Earthquake adapter boundary honestly and provenance no longer stamps rejected or failed REAL_ENGINE runs as executed engines. Science Chat can open the existing Scientific Memory history route directly; the route remains local-only and does not claim replay or cloud collaboration. The investor demo runbook records the real 5-minute flow, exact prompts, routes and proof boundaries for partner demonstrations. The CI whitespace gate ignores only markdown hard-breaks and continues checking code; the GIS barrel no longer exposes live `importOsmMap` while GIS is parked. Gemini proposals for bio-quantum, wildfire, grid, toxic dispersion and FEA remain PARK until independently validated. The Self-Driving Laboratory / Scientific Control Plane direction is accepted as a strategic research track, not implementation scope: its source descriptions, standards, APIs and market claims require independent verification. Before any instrument adapter, Genesis must specify and validate one instrument-agnostic observation contract that reuses Experiment Fabric, Evidence and Replay. The first PySCF Control Plane benchmark is now `CONNECTED_LOCALLY / CI_VERIFY_REQUIRED`: the local pinned PySCF 2.14.0 runtime executed both allowlisted H₂ basis arms through the canonical async backend path, generated a real Evidence Pack with arm reproduction `MATCH`, and passed a second real execution with stable fingerprints and pack identity. The existing Pilot Protocol/A-B UI now routes admitted backend models through that async executor; a fresh Chromium proof showed two real H₂ geometry arms (`-1.11675931` and `-1.11615145` Hartree) with `SUPPORTED_WITHIN_PROTOCOL` and `MATCH` per arm. Backend echo integrity now blocks tampered geometry/basis before completion. A canonical Capability Admission Matrix generator and release-gated test now derive status/engine/version/route/proof-boundary/limitations from the existing registry; PySCF is explicitly CONNECTED, while unproven backend models remain VERIFY_REQUIRED and hypothetical/seam models remain PARKED. Chromium smoke and CI are green for the product regression surface; persisted user-facing replay reload and CI PySCF runtime portability remain open. Next selection remains the smallest real gap that closes those gates, not a new scientific domain.

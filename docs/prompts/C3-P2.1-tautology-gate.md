# PROMPT DLA C3 — PAKIET P2.1: Tautology Gate (Genesis OS)

Gałąź: `claude/genesis-autonomous-completion-95bt4e`.

## NAJPIERW BLOKER, KTÓRY MUSISZ ZNAĆ

Brief mówi: „Zaimplementuj Tautology Gate **wg `docs/GENESIS_TAUTOLOGY_AND_EMPIRICAL_TEST_GATE.md`**
… oraz **25 golden test cases z §6 specu**".

**Ten plik NIE ISTNIEJE w repo.** Zweryfikowane:
```
git log --all --full-history --name-only --pretty=format: | grep -i tautolog
  → pusto (nigdy nie istniał w żadnej gałęzi ani w historii)
```
Słownik ze specu (`CONSISTENCY_CHECK`, `EMPIRICAL_TEST`, `MIXED_TEST`, `UNTESTABLE`,
`NON_DISCRIMINATING`, `DiscriminationRecord`) też nie występuje w kodzie.

**Nie wymyślaj 25 golden cases i nie podpisuj ich jako „z §6 specu".** To byłaby
fabrykacja dokładnie tego rodzaju, którego ta misja zabrania. Zażądaj specu od autora
briefu i dopóki go nie masz, pracuj wyłącznie nad częścią, która jest bezpieczna
(niżej), oznaczając reszcie status `BLOCKED — brak specu`.

## CO MOŻESZ ZROBIĆ BEZ SPECU (i co jest naprawdę wartościowe)

Genesis ma już zaimplementowane dokładnie to rozróżnienie — tylko ad hoc i lokalnie,
a nie jako jedną bramkę. Zacznij od AUDYTU, nie od kodu:

1. **Znajdź każde miejsce, gdzie repo już rozstrzyga „tautologia vs test empiryczny".**
   Punkt startowy, potwierdzony: `packages/frontend/src/core/agent/entanglementInquiry.ts`
   — stałe `QE1_NOT_MODELLED` i `QE2_NOT_MODELLED` mówią wprost, że granica Tsirelsona i
   nierówność CKW są ANALITYCZNYMI SUFITAMI liczonej algebry, więc żaden run ich nie
   testuje (hipoteza o źródle nadkwantowym jest NIEROZSTRZYGALNA, nie sfalsyfikowana).
   To jest istniejący, działający przykład tej klasyfikacji — i wzorzec, pod który
   bramka powinna pasować, zamiast wprowadzać własne, drugie pojęcie.
   Sprawdź też: `falsificationRelation.ts` (`applicable` vs `met`),
   `discoveryConclusion.ts` (kryterium nierozstrzygalne ≠ sfalsyfikowane),
   `discoveryAdmission.ts` (`AdmissionStatus`), `dataProvenance.ts`.
2. **Napisz `docs/TAUTOLOGY_GATE_AUDIT.md`**: gdzie to rozróżnienie już żyje, pod jakimi
   nazwami, i które z nich są tym samym pojęciem w różnych słownikach. Bez tego
   implementacja bramki będzie SIÓDMYM słownikiem epistemicznym w repo — patrz G6 w
   `docs/GENESIS_SCIENTIFIC_DISCOVERY_ENGINE_MASTER_PLAN.md`, gdzie sześć takich
   słowników już trafiło do jednego pola.
3. Dopiero z tym audytem w ręku zaproponuj kształt bramki — jako **czystą funkcję**
   klasyfikującą + **pola metadanych** na ISTNIEJĄCYCH rekordach. Zero nowych silników.

## ZASADY TWARDE
1. Dowód = komenda + wyjście + commit hash. Bez dowodu → `NOT VERIFIED`.
2. TDD: test na czerwono przed implementacją, z wklejonym czerwonym wyjściem.
3. **Zero nowych silników** (evidence/replay/memory/graph/epistemic/conflict) i zero
   nowych słowników epistemicznych. Reuse: `falsificationRelation.ts`,
   `discoveryConclusion.ts`, `epistemicReliability.ts`, `dataProvenance.ts`.
4. Nie łamiesz zapisanych danych (`SavedExperiment`, localStorage, SQLite).
5. **Osiągalność**: `moduleReachability.test.ts` odrzuci moduł bez realnego konsumenta
   produkcyjnego. Jeśli budujesz bramkę, od razu ją podłącz albo udokumentuj w allowliście
   z powodem — nie zostawiaj biblioteki, której nic nie czyta.
6. Pełna bramka przed pushem: eslint, tsc, oba suite'y, build.

## DEFINICJA DONE
- `docs/TAUTOLOGY_GATE_AUDIT.md` — gdzie to rozróżnienie już jest i pod jakimi nazwami.
- Propozycja kształtu bramki jako czysta funkcja + metadane, z jawnym mapowaniem na
  istniejące słowniki (co konsolidujemy, co zostaje osobno i dlaczego).
- Status `BLOCKED — brak specu` dla: reguł anty-cyrkularności C1–C6, `DiscriminationRecord`
  z priorytetem 0 dla `NON_DISCRIMINATING`, i 25 golden cases. **Nie zgaduj ich treści.**

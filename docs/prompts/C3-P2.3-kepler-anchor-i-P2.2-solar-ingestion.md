# PROMPT DLA C3 — kontynuacja: P2.3 druga kotwica (Kepler) + P2.2 ingestion Solar Mind

Gałąź: `claude/genesis-autonomous-completion-95bt4e` (u Ciebie już zmergowana — commit `5e6f304`
zawiera Twój `docs/TAUTOLOGY_GATE_AUDIT.md` i moje P0/P2.3/P3.2). Pakiet badawczy Qwena (Part A —
kotwica keplerowska; Part B — pełny tekst `SOLAR_MIND_MASTER_REPORT.md`/`SOLAR_MIND_EXPANSION.md`),
który dostałeś, trafił do Ciebie przez pomyłkę użytkownika, ale zostaje u Ciebie — wykonaj go.

## NAJPIERW PRZECZYTAJ, ŻEBY NIE ZBUDOWAĆ DRUGIEGO SYSTEMU

P2.3 **już ma zaimplementowany kontrakt**, na innej domenie (masa molowa kofeiny, PubChem):
- `packages/frontend/src/core/biotechData/externalAnchor.ts` — `ExternalAnchor`, `resolveExternalAnchor`,
  `buildAnchoredReferenceRun`, `runExternalAnchor`, `anchorPayloadDigest`. Wszystkie te funkcje są
  DOMENOWO NEUTRALNE (biorą `unknown` payload) — tylko `molecularWeightAnchor` i `asPubChemProperties`
  są specyficzne dla chemii. **Twoja kotwica keplerowska to DRUGI WPIS w `EXTERNAL_ANCHORS`, nie drugi
  kontrakt.** Nie kopiuj interfejsu, nie twórz `kepleranchor.ts` z własnym `resolveExternalAnchor`.
- `docs/P2_EVIDENCE.md` (sekcja P2.3) i `docs/REPRODUCIBILITY_PACK.md` — dowody wykonania pierwszej
  kotwicy, wzorzec dowodowy, który masz powtórzyć dla swojej.
- `docs/DECISIONS.md` D-011..D-014 — w D-014 opisany jest REALNY BŁĄD, który sam popełniłem: odcisk
  payloadu liczony Z PAYLOADU przy ładowaniu modułu był pozorny (zawsze się zgadzał). **Twój
  `payloadDigest` MUSI być literałem w źródle, przeliczonym raz i wklejonym**, nie
  `anchorPayloadDigest(twojePayload)` w deklaracji — dokładnie ten sam błąd czeka na powtórzenie.

## ZNALEZISKO, KTÓRE OSZCZĘDZA CI POŁOWĘ PRACY

Part A §2 mówi „jeśli w `router.ts` istnieje model keplerowski — użyć go, nie budować nowego". **Istnieje
i liczy dokładnie to, czego potrzebujesz:**

```
packages/frontend/src/core/modelGraph/orbitalGraph.ts::buildOrbitalModelGraph()
  wejścia:  centralMassSolar (M☉), orbitalRadiusAu (AU)
  wyjście:  orbitalPeriodYears = T = 2π√(a³/(G·M)), III prawo Keplera dokładnie, G=4π² w tych jednostkach
```

Zarejestrowany w Fabric jako `universe-kepler` (`router.ts:106-110`), wykonawca w
`executor.ts::case 'universe-kepler'`. **Nie pisz drugiej implementacji III prawa Keplera.** Twoja
predykcja = `orbitalPeriodYears(a, M) × 365.25` (dni), z `a` i `M` przypiętego payloadu — dokładnie
jak Twój §3 wymaga: predykcja liczona PRZED odczytaniem `pl_orbper`.

## GDZIE JEST GRANICA TWOJEJ WŁASNEJ SPEKULACJI — SPRAWDŹ, ZANIM COKOLWIEK ZAPISZESZ

Twój Part A §1 podaje **ILUSTRACYJNY** rekord CSV z jawną adnotacją „wartości do potwierdzenia live".
**Ten rekord NIE JEST realną obserwacją i nie wolno go przypiąć jako payload kotwicy.** To byłaby
dokładnie ta fabrykacja, której cała ta misja zabrania — ilustracja podpisana jako dowód.

Zanim zaczniesz kodować: **sprawdź sam, czy Twoje środowisko ma dostęp do
`exoplanetarchive.ipac.caltech.edu`.** Z mojego środowiska było ZABLOKOWANE przez politykę proxy
organizacji (`gateway answered 403 to CONNECT`; przechodzą wyłącznie rejestry pakietów — npm, pypi,
crates, itp.) — `docs/P2_EVIDENCE.md` ma pełny dowód tego bloku. Twoje środowisko może być inne; **zmierz,
nie zakładaj** (`curl -sS -o /dev/null -w "%{http_code}\n" --max-time 20 "https://exoplanetarchive.ipac.caltech.edu/TAP/sync?query=select+count(*)+from+ps&format=csv"`).

- **Jeśli masz dostęp**: pobierz realny CSV dla przypiętej listy planet (4 z §1 albo mniej, jeśli
  chcesz zacząć od jednej), zapisz go jako plik w repo obok kotwicy (wzorzec: `pubchem-cid-2519.json`
  leży obok `externalAnchor.ts`), z URL-em zapytania, datą pobrania, i policz odcisk PO zapisaniu.
- **Jeśli NIE masz dostępu**: zgłoś to jako `NOT VERIFIED` w `docs/P2_EVIDENCE.md`, dokładnie tak, jak
  ja to zrobiłem dla live ingestion. **Nie przypinaj rekordu ilustracyjnego.** W takim razie ta część
  zadania (kotwica keplerowska) zostaje `BLOCKED — brak dostępu do źródła`, a Ty przechodzisz do Part B
  (ingestion Solar Mind), który nie wymaga sieci.

## CO ZROBIĆ, JEŚLI DOSTĘP JEST (implementacja kotwicy)

1. **TDD, test na czerwono najpierw** — wzorzec: `externalObservationAnchor.test.ts`. Musi pokrywać
   to samo, co tam: odmowa przy zmienionym payloadzie (na LITERALE odcisku, nie na wyliczeniu — patrz
   D-014), realna falsyfikacja (predykcja celowo zła musi dostać `FALSIFIED_WITHIN_PROTOCOL`), odrzucenie
   runu `SIMULATED`, replay `MATCH`, i asercja `toBe('<literał>')` na odcisku.
2. **Pasmo**: Twój §4 (propagacja z `pl_orbpererr1` + błędów `a`/`M★`) jest metodologicznie poprawny —
   zaimplementuj dokładnie tak, prerejestrowany PRZED odczytem `pl_orbper`, tak jak w kryterium.
3. **Sekcja „co to POZOSTAJE tautologią"** — masz to już napisane w §6 i jest dobre: rozjazd `a`+`M★`
   (łańcuch RV+modele stelarne) vs `pl_orbper` (pomiar zegarowy z tranzytów) to prawdziwa reszta
   empiryczna; reszta to sprawdzanie własnej arytmetyki i spójności archiwum. Przenieś to DOSŁOWNIE do
   pola `whatThisTests`/`whatRemainsUntested` w deklaracji kotwicy — to jest wzorzec z `molecularWeightAnchor`.
4. **Wygeneralizuj rendering na `#/evidence`.** Dziś `EvidenceShowcaseScreen.tsx::ExternalAnchorSection`
   hardkoduje `MOLECULAR_WEIGHT_ANCHOR_ID` (linia 68-70) — druga kotwica w `EXTERNAL_ANCHORS` NIE
   wyrenderuje się, dopóki tego nie zmienisz na iterację po całej liście. Zrób to Ty, jako część tego
   zadania (mapuj `EXTERNAL_ANCHORS` → sekcja na kotwicę, zamiast `.find(id === STAŁA)`).
5. **Reachability guard**: `moduleReachability.test.ts` złapie nowy moduł/eksport bez realnego
   konsumenta — dokładnie jak złapał mój `epistemicReliability.ts` w commicie `be5f222`. Podpięcie do
   ekranu z punktu 4 to i tak realne podpięcie.
6. **`scripts/repro-demo.mjs`**: dodaj swoją kotwicę do `EXPECTED` i do wypisywanych sprawdzeń —
   `reproEntry.node.ts` już eksportuje wzorzec (`reproExternalAnchor`), Twoja wersja to analogiczna
   funkcja dla nowego `anchorId`, nie nowy plik fasady.
7. **Aktualizuj `docs/RISKS.md` R-005** i **`docs/P2_EVIDENCE.md`** tym samym stylem dowodowym: komenda +
   wyjście + hash commita, w tym pełny cykl (predykcja → obserwacja → werdykt → replay → co pozostaje
   nieprzetestowane), i realny dowód wizualny z Chromium na `#/evidence` z DWIEMA kotwicami widocznymi.

## PART B — P2.2, ingestion Solar Mind (nie wymaga sieci, rób niezależnie od wyniku Part A)

1. Zapisz oba pliki DOSŁOWNIE, tak jak dostałeś: `knowledge/SOLAR_MIND_MASTER_REPORT.md` i
   `knowledge/SOLAR_MIND_EXPANSION.md`, z nagłówkiem `STATUS NADRZĘDNY: RESEARCH ONLY / NOT RUN`
   zachowanym na górze każdego. Listing `solarVisualizer.ts` z pierwotnej emisji EXPANSION **nie wchodzi**
   — to kod, nie dane, i sam pakiet to mówi wprost.
2. Dodaj tabelę statusu H051–H056 **dokładnie na wzór** tabeli QE1–QE7 w `knowledge/quantum.md:228-238`
   (ten sam format: ID | Hipoteza | Falsyfikator | Status). Adnotacja `ADNOTACJA` przy każdej H051–H056
   z pakietu (RESEARCH ONLY/NOT RUN, „nie wchodzi do pętli jako evidence dopóki brak ingestii i
   prerejestracji") ma zostać, dosłownie — to jest dokładnie dyscyplina, której trzymał się QE1.
3. **Nie buduj pipeline'u, twina ani adaptera solvera.** Part A §9-10 tego pakietu Qwena projektuje
   architekturę na przyszłość ("CLAUDE SHOULD IMPLEMENT") — to jest materiał na OSOBNE zadanie po tym,
   jak dane wejdą do repo, nie coś do zrobienia teraz. P2.2 kończy się na wpisaniu danych z etykietą.
4. Zaktualizuj `docs/MASTER_PRIORITY_GENESIS.md`, sekcja o otwartych zadaniach Qwena/Solar —
   odhacz P2.2 jako zrobione i zapisz, że H051–H056 nadal czekają na przejście przez `StrategyRun`
   (to jest kolejne, osobne zadanie, analogiczne do QE1→QE3).

## ZASADY TWARDE (te same co zawsze)
1. Dowód = komenda + wyjście + commit hash. Bez dowodu → `NOT VERIFIED`, nigdy prezentowane jako done.
2. TDD: test na czerwono przed implementacją.
3. Zero nowych silników/kontraktów tam, gdzie istniejący się nadaje — masz dwa potwierdzone: kontrakt
   kotwicy (`externalAnchor.ts`) i model keplerowski (`orbitalGraph.ts`/`universe-kepler`).
4. Żadnej wartości ilustracyjnej/wymyślonej przypiętej jako obserwacja zewnętrzna.
5. Pełna bramka przed pushem: eslint, tsc, oba suite'y, build, `node scripts/repro-demo.mjs`.

## DONE
- Kotwica keplerowska ZAIMPLEMENTOWANA z realnymi danymi, ALBO jawnie `BLOCKED — brak dostępu do źródła`
  w `docs/P2_EVIDENCE.md` z dowodem próby (kod HTTP).
- `knowledge/SOLAR_MIND_*.md` w repo, dosłowne, z tabelą H051–H056.
- `#/evidence` renderuje WSZYSTKIE kotwice z `EXTERNAL_ANCHORS`, nie jedną na sztywno.
- `docs/RISKS.md` R-005 i `docs/MASTER_PRIORITY_GENESIS.md` zaktualizowane z prawdziwym stanem.

# Ligand/receptor preparation for AutoDock Vina, pinned to the version verified
# in this repo (packages/backend/src/compute/dockingAdapter.mjs / dock_worker.py).
# Meeko depends on RDKit; the "structural" worker installs requirements-rdkit.txt
# alongside this file so both land in the same interpreter.
meeko==0.8.0
# meeko 0.8.0 declares NO dependencies in its package metadata, yet `import meeko`
# imports scipy (molecule_pdbqt.py, receptor_pdbqt.py) and gemmi (chemtempgen.py).
# A clean install without these two left docking BLOCKED_BY_RUNTIME
# ("No module named 'scipy'", then "... 'gemmi'"); pinned to the verified versions.
scipy==1.17.1
gemmi==0.7.5
# Optional receptor repair in dock_worker.py (imported lazily, only when the request asks
# for it): adds missing side-chain atoms and terminal oxygen, never missing loops.
# NOTE: pdbfixer publishes no wheel on PyPI, so the structural image installs it with an
# explicit --no-binary=pdbfixer; it is pure Python and needs no compiler. See
# workers/structural/Dockerfile.
pdbfixer==1.12.0

# RETROSYNTHESIS ROUTE SEARCH — AiZynthFinder (MolecularAI, MIT), the engine behind
# packages/backend/src/compute/retro_worker.py.
#
# It MUST live in its own interpreter: aizynthfinder pins rdkit<2024, while the docking/QM stack in
# this repo runs RDKit 2026.x. Install into a separate venv and point GENESIS_RETRO_PYTHON at it —
# installing this beside requirements-rdkit.txt would downgrade RDKit under the docking worker.
#
# Verified in this repo on 2026-09-26: the packages below install and `from
# aizynthfinder.aizynthfinder import AiZynthFinder` succeeds (aizynthfinder 4.4.1, rdkit 2023.9.6,
# rdchiral 1.1.0, onnxruntime 1.30.0, numpy 1.26.4).
#
# MODEL DATA IS NOT INSTALLED BY PIP AND IS NOT SHIPPED WITH GENESIS. The expansion-policy network,
# the template library and the purchasable stock (~1 GB, own upstream licences) are published by the
# authors on zenodo/figshare; `python -m aizynthfinder.tools.download_public_data <dir>` fetches them.
# Point GENESIS_RETRO_MODEL_DIR at that directory. Until then the engine reports BLOCKED_BY_RUNTIME
# with the missing filenames, and Genesis proposes no route at all.
#
# Installed with --no-deps plus the runtime dependencies below, so the notebook/plotting extras
# (jupyter, matplotlib, seaborn) that aizynthfinder declares for its GUI are left out; nothing in the
# Genesis worker imports them.
aizynthfinder==4.4.1
rdkit==2023.9.6
rdchiral==1.1.0
onnxruntime==1.30.0
numpy==1.26.4
pandas==2.3.3
networkx==2.8.8
tables==3.11.1
deprecated==1.3.1
paretoset==1.2.5
jinja2==3.1.6
requests==2.34.2
pillow==12.3.0
reaction-utils==1.9.3

import { describe,it,expect } from 'vitest'; import { deterministic } from '../deterministic'; import { compileScientificInterior } from './interiorLayoutCompiler';
describe('V6 scientific interiors',()=>{it('compiles deterministically',()=>{const spec={interiorId:'x',roomKind:'IMAGING' as const,bounds:{widthM:8,depthM:6,heightM:3},seed:'s',requestedCapabilities:['microscopy'],provenance:[]}; const a=compileScientificInterior(spec,deterministic),b=compileScientificInterior(spec,deterministic); expect(a.fingerprint).toBe(b.fingerprint); expect(a.assets.length).toBeGreaterThan(0);});});
